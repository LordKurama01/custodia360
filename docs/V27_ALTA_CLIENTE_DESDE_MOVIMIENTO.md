# Custodia360 v27 — Alta cliente desde movimiento

Corrección urgente de flujo operativo:

- En **Nuevo movimiento**, el selector de cliente ahora incluye `＋ Agregar cliente nuevo`.
- También se agregó botón visible `＋ Agregar cliente` debajo/junto al selector.
- Si el cliente no existe, se crea desde ahí mismo.
- Al guardar, vuelve automáticamente al movimiento y deja ese cliente seleccionado.
- No se agregan módulos nuevos; se corrige el flujo real de carga.
